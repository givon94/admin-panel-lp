extends ../_layout

//-block title-description
    -var title = 'Заголовок'
    -var descr = 'Описание'

    title= title
    meta(name='title', content=title)
    meta(name='description', content=descr)

    -var thisPage = 'Главная';
    -var thisUrl = 'index'

//-block open-graph
    +open-graph(title, descr, `${thisUrl}.html`, `og-${thisUrl}`)


block header
    //- Шапка
    include ../blocks/_header

block main

    //- Вопросы
    include ../blocks/_questions
